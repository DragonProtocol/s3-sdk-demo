import { LoginButton } from "@us3r-network/auth-react";
import App from "../app/App";
const Page: React.VFC = () => {
  return (
    <App>
      <LoginButton />
    </App>
  );
};

export default Page;
